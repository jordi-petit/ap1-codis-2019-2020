/* Programa que suma dos enters */

#include <iostream>
using namespace std;

int main() {
    int x, y;               // declaració de variables
    cin >> x >> y;          // lectura de les dades
    int s = x + y;          // càlcul del resultat
    cout << s << endl;      // escriptura del resultat
}
